import React, { StrictMode } from 'react';
import { Counter } from 'Containers';
// import { app } from "./App.scss";

const App = () => {
  return (
    <StrictMode>
      <Counter />
    </StrictMode>
  );
};

export default App;
